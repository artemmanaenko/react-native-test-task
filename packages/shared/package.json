{
  "name": "@orders/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "jest --config jest.config.cjs --watchman=false",
    "test:coverage:check": "jest --config jest.config.cjs --watchman=false --coverage --coverageReporters=text-summary --coverageThreshold='{\"global\":{\"lines\":50,\"statements\":50,\"functions\":40,\"branches\":30}}'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint skipped for shared'"
  },
  "peerDependencies": {
    "react": ">=18",
    "@tanstack/react-query": ">=5"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "jest": "29.7.0",
    "ts-jest": "29.2.5",
    "typescript": "5.7.2"
  }
}
